# Namecheap Registration, Cloudflare DNS

`coinarchive.app` will remain registered with Namecheap, while its authoritative DNS will be delegated to Cloudflare. This preserves domain ownership and billing at the registrar while allowing Cloudflare to manage the application routes, custom image hostnames, TLS, and private staging access.
