# GitHub Production Environment Approval

Production promotion will use a protected GitHub production environment that only the maintainer can approve after manual staging verification. This gives each production deployment an auditable, explicit approval step without a separate release system.
