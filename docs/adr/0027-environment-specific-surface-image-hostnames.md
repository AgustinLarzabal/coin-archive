# Environment-Specific Surface Image Hostnames

Production Surface Images will use `images.coinarchive.app`; private staging Surface Images will use `images.staging.coinarchive.app`. These stable, environment-specific hostnames keep public production image URLs distinct from protected staging assets.
