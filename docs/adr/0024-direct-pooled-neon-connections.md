# Direct Pooled Neon Connections

Cloudflare Workers will initially connect directly to their environment’s Neon pooled PostgreSQL endpoint with Workers Node.js compatibility enabled. Cloudflare Hyperdrive is deferred until measured connection latency or scale justifies another managed layer and its potential cost.
