# Explicit Initial Admin Bootstrap

Each environment will establish its initial Admin through a one-time, manually invoked bootstrap workflow that promotes a specified Google-authenticated Collector after sign-in. The application will not grant Admin based on sign-in order, preventing an unintended first visitor from gaining access-management authority.
