# Google-Only Collector Sign-In

Coin Archive will initially offer Google as its only Collector sign-in provider in both staging and production. This keeps OAuth configuration small and aligns staging access with the maintainer's approved Google identity; additional providers may be introduced later if needed.
